using System;
using System.Collections.Generic;
using System.Drawing;
using System.Linq;
using System.Text;
using System.Threading.Tasks;
using PrintDriverNet;

namespace ConsoleTst.Infrastructure
{
    class Class1
    {
        PrintDriverNet.Printer.PrinterSettings ps = new Printer.PrinterSettings();
        public void start()
        {
            //Printer p = new Printer();
            //int status = -100;
            //p.ReadStatus(out status);
            //Console.Write(status);
            //p.StartDoc("sre");
            //p.StartPage();
            //Bitmap bm = new Bitmap(Image.FromFile(@"D:\1.bmp"));
            //IntPtr handle = bm.GetHbitmap();
            //p.LoadLogoFromBitmap(handle);
            //p.EndPage();
            //p.EndDoc(false);
        }
    }
}
